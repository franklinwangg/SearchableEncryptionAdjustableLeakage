How to run code : 
1) download the Chicago crime database, and paste it so it's in the root directory on the same level as "main.py"
2) run "python main.py" in the terminal, and all the tests should execute